enum Color:byte { Red = 0, Blue, Green, Purple, Black, Orange, Cyan, Pink = 7}

struct Vector2 {
    x: float32;
    y: float32;
}

table Player {
    pos: Vector2;
    vel: Vector2;
    acc: Vector2;
    color: Color = Red;
}

table PlayersList {
  players: [Player];
}

root_type PlayersList;
