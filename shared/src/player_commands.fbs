enum PlayerCommand:uint8 { MoveRight, MoveLeft, Jump, Disconnect }

table PlayerCommands {
    sequence: uint32;
    dt_micro: uint64;
    commands: [PlayerCommand];
    client_timestamp_micro: uint64;
}

root_type PlayerCommands;
